<!-- Copyright (c) Microsoft Corporation and Contributors. -->
<!-- Licensed under the MIT License. -->

<Window
    x:Class="Hutao.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Hutao"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Snap.Hutao.Remastered.Deployment">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid x:Name="AppTitleBar" Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <!-- Logo -->
            <Image Grid.Column="0" 
                   Source="ms-appx:///Assets/InAppLogo.png" 
                   Width="128" 
                   Height="128" 
                   Margin="16,16"
                   VerticalAlignment="Center"/>
            
            <TextBlock Grid.Column="1" 
                       x:Name="tbAppTitle" 
                       Text="Snap.Hutao Installer" 
                       Foreground="White"
                       FontSize="14"
                       FontWeight="SemiBold"
                       VerticalAlignment="Center"
                       Margin="8,0"/>
        </Grid>

        <Grid Grid.Row="1" Padding="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- 欢迎文本 -->
            <TextBlock Grid.Row="0" 
                       Text="欢迎使用 Snap.Hutao.Remastered 安装器" 
                       FontSize="20"
                       FontWeight="SemiBold"
                       Margin="0,0,0,8"/>

            <Grid Grid.Row="1" Margin="0,0,0,8">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
                    <TextBlock Text="本地版本：" FontWeight="SemiBold"/>
                    <TextBlock x:Name="tbLocalVersion" Text="未安装" Margin="8,0,0,0"/>
                </StackPanel>
                
                <StackPanel Grid.Row="1" Orientation="Horizontal">
                    <TextBlock Text="最新版本：" FontWeight="SemiBold"/>
                    <TextBlock x:Name="tbLatestVersion" Text="正在获取..." Margin="8,0,0,0"/>
                </StackPanel>
            </Grid>

            <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,8">
                <TextBlock Text="状态：" FontWeight="SemiBold"/>
                <TextBlock x:Name="tbStatus" Text="正在检查更新..." Margin="8,0,0,0"/>
            </StackPanel>

            <!-- 进度显示区域 -->
            <Border Grid.Row="3" 
                    BorderBrush="{ThemeResource SystemControlForegroundBaseLowBrush}" 
                    BorderThickness="1" 
                    CornerRadius="4"
                    Padding="16"
                    Margin="0,0,0,24"
                    Visibility="Collapsed"
                    x:Name="progressArea">
                <StackPanel>
                    <TextBlock x:Name="tbProgressTitle" 
                               Text="正在下载..." 
                               FontSize="16"
                               FontWeight="SemiBold"
                               Margin="0,0,0,8"/>
                    
                    <ProgressBar x:Name="pbProgress" 
                                 Minimum="0" 
                                 Maximum="100" 
                                 Value="0"
                                 Height="8"
                                 Margin="0,0,0,8"/>
                    
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <TextBlock Grid.Column="0" 
                                   x:Name="tbProgressText" 
                                   Text="0%"/>
                        
                        <TextBlock Grid.Column="1" 
                                   x:Name="tbProgressSpeed" 
                                   Text=""/>
                    </Grid>
                </StackPanel>
            </Border>

            <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="btnAction" 
                        Content="检查更新" 
                        Click="btnAction_Click"
                        Style="{StaticResource AccentButtonStyle}"
                        MinWidth="120"
                        Height="36"/>
                
                <Button x:Name="btnCancel" 
                        Content="取消" 
                        Click="btnCancel_Click"
                        Margin="12,0,0,0"
                        MinWidth="80"
                        Height="36"
                        Visibility="Collapsed"/>
            </StackPanel>
        </Grid>

        <Border Grid.Row="2" 
                BorderBrush="{ThemeResource SystemControlForegroundBaseLowBrush}" 
                BorderThickness="0,1,0,0"
                Padding="12,8">
            <TextBlock x:Name="tbFooter" 
                       Text="Snap.Hutao Remastered Deployment v1.0.0" 
                       Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                       FontSize="12"/>
        </Border>
    </Grid>
</Window>
